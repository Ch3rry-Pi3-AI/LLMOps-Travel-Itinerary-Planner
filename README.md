# 🏗️ **Initial Project Setup — LLMOps Travel Itinerary Planner**

This branch establishes the **foundational structure** for the **LLMOps Travel Itinerary Planner**.
It sets up a **modular Python codebase**, configures **dependency management with `uv`**, and prepares the repository for future stages — including **itinerary generation**, **LLM-powered travel recommendations**, and **interactive user interfaces**.

## 🗂️ **Project Structure**

```text
LLMOPS-TRAVEL-ITINERARY-PLANNER/
├── .venv/                                # 🧩 Virtual environment managed by uv
├── .env                                  # 🔑 Environment variables (API keys, tokens)
├── .gitignore                            # 🚫 Git ignore rules
├── .python-version                       # 🐍 Python version pin
├── llmops_travel_itinerary_planner.egg-info/  # 📦 Package metadata (auto-generated)
├── pyproject.toml                        # 🧩 Project metadata and uv configuration
├── requirements.txt                      # 📦 Python dependencies
├── setup.py                              # 🔧 Editable install configuration
├── uv.lock                               # 🔒 Dependency lock file (auto-generated by uv)
├── main.py                               # 🚀 Entry point for running the application
├── src/                                  # 🧠 Core source code for the project
│   ├── chains/                           # 🔄 (Placeholder) Workflow logic for itinerary generation
│   ├── core/                             # 🧩 (Placeholder) Core LLM orchestration and reasoning logic
│   ├── config/                           # ⚙️ Configuration management
│   │   ├── config.py                     # Loads environment variables (e.g., Groq API key)
│   │   └── README.md                     # Documentation for configuration module
│   └── utils/                            # 🪵 Logging and exception utilities
│       ├── custom_exception.py           # Unified exception handling across modules
│       ├── logger.py                     # Centralised logging configuration
│       └── README.md                     # Documentation for utilities
└── README.md                             # 📖 Project documentation (you are here)
```

> 💡 **Note:** The `.env` file contains sensitive API keys and must **never** be committed to version control.

## ⚙️ **Setup Process**

### 1️⃣ Create the Project Structure

A modular structure was designed for **clarity**, **scalability**, and **future LLM integration**, with separate folders for configuration, utilities, and future core logic.

```bash
mkdir -p src/{chains,core,config,utils}
touch src/config/config.py src/utils/{custom_exception.py,logger.py}
```

Key modules were implemented in `src/config/` and `src/utils/`:

* `config.py` — Loads and manages environment variables (e.g., Groq API key).
* `custom_exception.py` — Provides consistent, context-rich exception handling.
* `logger.py` — Implements structured, time-stamped logging for debugging and traceability.

### 2️⃣ Create and Activate the Virtual Environment (with `uv`)

The project uses [`uv`](https://github.com/astral-sh/uv) for **fast, reproducible environments**.

Create and activate the environment (Python 3.12):

```bash
uv venv --python 3.12
```

* **Windows (cmd):**

  ```cmd
  .\.venv\Scripts\activate
  ```
* **PowerShell:**

  ```powershell
  .\.venv\Scripts\Activate.ps1
  ```
* **macOS / Linux:**

  ```bash
  source .venv/bin/activate
  ```

### 3️⃣ Define and Install Dependencies

An initial `requirements.txt` was created for early development:

```text
python-dotenv
langchain
langchain-core
langchain-community
langchain-groq
```

Install and lock dependencies:

```bash
uv pip install -r requirements.txt
uv lock
```

## 🔐 **Environment Variable Setup (.env)**

Before running the application, create a `.env` file in the project root to store API credentials.

Create the file:

```bash
touch .env
```

Then add the following entry:

```text
GROQ_API_KEY=""
```

### 🧠 **Where to Get the Groq API Key**

Go to [https://console.groq.com/](https://console.groq.com/) and sign in or create an account.
Generate a new API key and paste it into the `GROQ_API_KEY` field.

## 🧩 **Integration Guidelines**

| Folder        | Purpose                                                                 |
| ------------- | ----------------------------------------------------------------------- |
| `src/config/` | Central configuration management for API keys and environment settings. |
| `src/utils/`  | Logging and unified exception-handling utilities.                       |
| `src/core/`   | Placeholder for main LLM logic and itinerary reasoning (future use).    |
| `src/chains/` | Placeholder for travel workflow chains and orchestration.               |

✅ **In summary:**
This setup establishes a **clean, modular, and extensible foundation** for the **LLMOps Travel Itinerary Planner**, supporting:

* Secure environment management through `.env`
* Centralised configuration and logging utilities
* Reproducible environments with `uv`
* Clear separation between configuration, logic, and utilities
